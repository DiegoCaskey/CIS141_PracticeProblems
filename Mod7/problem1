# 1. Write a function called count_vowels(input) that takes a string
# and returns the number of vowels (a, e, i, o, u) in it.
word = str(input("Give me a word!: "))
def count_vowels(word):
    counter = 0
    for letter in word:
        if letter == 'a' or letter == 'e' or letter == 'i' or letter == 'o' or letter == 'u':
            counter += 1
    return counter
vowels = count_vowels(word)
print(f"There are {vowels} vowels in this letter!")
